import styled from "styled-components";

export const Title = styled.h1`
  font-weight: 600;
  font-size: 40px;
  line-height: 60px;

  color: var(--black);
`;

export const Author = styled.span`
  font-size: 12px;
  line-height: 18px;

  color: var(--black);
`;
