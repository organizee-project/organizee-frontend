/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "conteudo.imguol.com.br"],
  },
};

module.exports = nextConfig;
